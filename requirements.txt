# Election Management Platform — install from repository root:
#   python3 -m pip install -r requirements.txt
#
# The shared `election_platform` package is source under shared/ (not an editable
# pip install). Run apps via deployment/scripts/* or set PYTHONPATH, e.g.:
#   export PYTHONPATH="shared:backend"   # backend
#   export PYTHONPATH="shared:website"   # website
#   export PYTHONPATH="shared:desktop"   # desktop
#
# Component-only installs:
#   python3 -m pip install -r backend/requirements.txt
#   python3 -m pip install -r website/requirements.txt
#   python3 -m pip install -r desktop/requirements.txt

-r backend/requirements.txt
-r website/requirements.txt
-r desktop/requirements.txt
